import React from 'react';
import {
	HeaderView,
    HeaderTitle,
} from './Style';



const Header = () => {
	return(
        <HeaderView>
            <HeaderTitle>Cadastrar Aterro</HeaderTitle>
        </HeaderView>
    );
};

export default Header;